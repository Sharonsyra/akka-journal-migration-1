akka {
  grpc {
    client {
      "namely.chief_of_state.ChiefOfStateService" {
        use-tls = false
        port = 9000
        port = ${?COS_PORT}
        host = "localhost"
        host = ${?COS_HOST}
      }
    }
  },
  http.server.preview.enable-http2 = on
}
